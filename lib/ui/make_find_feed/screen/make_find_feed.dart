import 'package:flutter/material.dart';

class MakeFindFeed extends StatelessWidget {
  const MakeFindFeed({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("MakeFindFeed"),
      ),
    );
  }
}
